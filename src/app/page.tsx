import Header from '@/components/Header/Header'

const Home = () => {
	return (
		// TODO: Add different page titles and meta (if possible)
		<div className='relative min-h-screen'>
			<Header />
		</div>
	)
}

export default Home
